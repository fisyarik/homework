SELECT
    Название,
    Континент,
    Население
FROM Страна
WHERE
    Континент = 'Южная Америка'
    AND Население > ALL (
        SELECT Население
        FROM Страна
        WHERE Континент = 'Африка'
    );